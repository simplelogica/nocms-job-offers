<h1><%= t '.job_offers_title' %></h1>
<ul>
  <% @job_offers.each do |job_offer| %>
    <li>
      <h2><%= job_offer.title %></h2>
      <%= raw job_offer.description %>
      <%= link_to t('.read_more', title: job_offer.title), job_offer_path(job_offer) %>
    </li>
  <% end -%>
</ul>
